namespace GeoPharma.Enums;

public enum TipoUsuario
{
    Admin = 1,
    Vendedor = 2
}

public enum TipoEstabelecimento
{
    Farmacia = 1,
    Drogaria = 2
}

public enum StatusEstabelecimento
{
    Prospect = 1,
    Cliente = 2
}